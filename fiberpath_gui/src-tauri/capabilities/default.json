{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default permissions for FiberPath GUI",
  "windows": ["main"],
  "permissions": [
    "shell:allow-open",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
